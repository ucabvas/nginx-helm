apiVersion: v2
name: nginx
description: A Helm chart that deploys NGINX on Kubernetes

# A chart can be either an 'application' or a 'library' chart.
#
# Application charts are a collection of templates that can be packaged into versioned archives
# to be deployed.
#
# Library charts provide useful utilities or functions for the chart developer. They're included as
# a dependency of application charts to inject those utilities and functions into the rendering
# pipeline. Library charts do not define any templates and therefore cannot be deployed.
type: application

# Chart version. Versioned INDEPENDENTLY of the nginx image: bump this for any
# change to the chart itself (templates, values, defaults), even when the nginx
# version is unchanged. chart-releaser publishes a release per chart version, so
# this must increase on every chart change. Has no relation to upstream nginx tags.
# Versions are expected to follow Semantic Versioning (https://semver.org/)
version: 0.1.0

# The nginx image version this chart deploys. The Deployment uses this as the
# image tag when image.tag is left empty, so it must be a concrete numeric nginx
# tag that exists upstream (never "latest"). Bump independently of `version`.
# It is recommended to use it with quotes.
appVersion: "1.27.3"

keywords:
  - nginx
  - web
  - http
  - proxy
home: https://nginx.org
sources:
  - https://github.com/nginx/nginx
  - https://hub.docker.com/_/nginx
maintainers:
  - name: ucabvas
